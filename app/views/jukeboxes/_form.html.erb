<%= form_for(@jukebox) do |f| %>
  <% if @jukebox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jukebox.errors.count, "error") %> prohibited this jukebox from being saved:</h2>

      <ul>
      <% @jukebox.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :list %><br>
    <%= f.text_field :list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
